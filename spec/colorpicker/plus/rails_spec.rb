require 'spec_helper'

describe Colorpicker::Plus::Rails do
  it 'has a version number' do
    expect(Colorpicker::Plus::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
